server:
  port: 8081
  servlet:
    context-path: /
    forward-headers-strategy: framework

spring:
  application:
    name: order-api
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://order-hub-postgres:5432/orderdb
    username: postgres
    password: postgres

app:
  jwt:
    # Signing key for HS512 algorithm
    # In http://www.allkeysgenerator.com/ you can generate all kinds of keys
    secret: v9y$B&E)H@MbQeThWmZq4t7w!z%C*F-JaNdRfUjXn2r5u8x/A?D(G+KbPeShVkYp
    expiration:
      minutes: 10
  cors:
    allowed-origins: https://order.greeta.net

logging:
  level:
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
